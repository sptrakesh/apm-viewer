<?xml version="1.0" encoding="UTF-8"?>
<messages xmlns:if="Wt.WTemplate.conditions">
  <message id="apm.page.tree">
    <div class="page">
      ${<if:full>}
      <div>
        <span>Number of APM records: ${rows}</span>
        <span>${submit}</span>
      </div>
      ${</if:full>}
      <div class="treeview">
        ${tree}
      </div>
      <div>
        <div><strong>Full APM details</strong></div>
        ${table}
      </div>
      <div>${error}</div>
    </div>
  </message>
</messages>
