name: Web

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest-arm

    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: web/docker/build.sh
